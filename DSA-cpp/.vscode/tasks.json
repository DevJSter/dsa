{
 "version": "2.0.0",
 "tasks": [
    {
       "label": "Run",
       "type": "shell",
       "command": "${workspaceFolder}/patterns",  // Update this line with the correct executable name
       "group": {
          "kind": "build",
          "isDefault": true
       }
    }
 ]
}
